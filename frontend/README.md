# Frontend (Angular)

See the repository root **`README.md`** for stack overview, prerequisites, and how to run the API and this app together.

```bash
npm install
npm start
```

- Dev server: `http://localhost:4200`
- Production build: `npm run build` (output under `dist/`)
- Unit tests: `npm test` / `ng test`
